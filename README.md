# Projet "Cinetech"

Le but de ce projet est de réaliser une bibliothèque de films et de séries, en utilisant l'API de TMDB.

### LANCEMENT

Pour lancer ce projet:

- Clonez le repo (`git clone https://github.com/cosmin-bilga/cinetech.git`)
- Installez python version 3 ([Telecharger Python](https://www.python.org/downloads/))
- Lancez le serveur avec python en vous mettant à la racine du projet (`python -m http.server 8000`)
- Accedez le site sur le navigateur (http://localhost:8000)
- Alternativement vous pouvez utiliser l'extension Live Server de VSCode

### MODIFICATION

Si vous voulez modifier le projet:

- Clonez le repo (`git clone https://github.com/cosmin-bilga/cinetech.git`)
- Installez typescript (`npm install -g typescript`)
- Lancez le compilateur typescript en mode watch (`npx tsc --watch`)
- Installez le CLI Tailwind (`npm install tailwindcss @tailwindcss/cli`)
- Lancez le watcher de tailwind (`npx @tailwindcss/cli -i index.css -o ./dist/output.css --watch`)

### LICENCE

Ce code est fourni tel quel, sans garantie d’aucune sorte, explicite ou implicite, cosmique ou interdimensionnelle.

Vous êtes libre de l’utiliser, le modifier, le distribuer, le vendre, le sacrifier à des divinités obscures, ou l’intégrer dans un grille-pain connecté.

En contrepartie, vous acceptez que l’auteur ne puisse en aucun cas être tenu responsable de quoi que ce soit, y compris mais sans s’y limiter :

- bugs étranges apparaissant uniquement les jours de pluie,
- comportements inexplicables après minuit,
- ouverture de portails dimensionnels,
- invasion de canards hostiles,
- ou déclenchement involontaire d’attaques nucléaires.

Si ce code devient conscient, merci de ne pas l’encourager.

Bonne chance.
